pipeline { agent any tools { pipeline {
    agent any

    tools {
        jdk 'jdk17'          // Matches name in Jenkins Global Tool Configuration
        nodejs 'node23'      // Matches installed NodeJS tool in Jenkins
    }

    environment {
        SCANNER_HOME = tool 'sonar-scanner'
    }

    stages {

        stage('Clean Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Check Java & Node') {
            steps {
                sh 'java -version'
                sh 'javac -version'
                sh 'node -v'
                sh 'npm -v'
            }
        }

        stage('Git Checkout') {
            steps {
                git 'https://github.com/
